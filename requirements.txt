# Core AI/ML Framework
langchain>=0.2.0
langchain-openai>=0.1.7
langgraph>=0.0.60
langsmith>=0.1.75
langchain-core>=0.2.0

# OpenAI Integration
openai>=1.30.1

# FastAPI Backend (for UI integration)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0
python-multipart>=0.0.6

# ── Speech Processing ──────────────────────────────────────────────────────────
# pyaudio==0.2.14    REMOVED: requires portaudio C library not available on Render
# pyttsx3==2.90      REMOVED: requires local audio hardware (headless server)
# SpeechRecognition==3.10.4  REMOVED: mic input not available server-side
# Speech is handled entirely client-side via the Web Speech API in the browser.
# If server-side STT/TTS is needed in future, use openai Whisper API instead:
#   openai>=1.30.1 already covers this via client.audio.transcriptions.create()

# Location Services
geocoder==1.38.1
geopy==2.4.1

# HTTP/API Clients
requests==2.32.3
httpx==0.27.0

# Data Processing
python-dotenv==1.0.1
pyyaml==6.0.1
pydantic==2.7.1

# Testing
pytest==8.2.1
pytest-cov==5.0.0
pytest-mock==3.14.0
pytest-asyncio==0.23.7

# Utilities
python-dateutil==2.9.0
colorama==0.4.6  # For colored console output on Windows
rich==13.7.1  # Enhanced terminal output

# Logging and Monitoring
loguru==0.7.2

# Development Tools
black==24.4.2
flake8==7.0.0
mypy==1.10.0
isort==5.13.2
